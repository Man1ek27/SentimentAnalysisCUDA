# Server requirements — no google-colab dependency
# Install with: pip install -r requirements_server.txt

torch>=2.1.0
transformers>=4.40.0
datasets
peft>=0.10.0
accelerate>=0.27.0
pandas
kaggle
tqdm
